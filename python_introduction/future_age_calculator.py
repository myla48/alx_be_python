#!/usr/bin/env python3

# Prompt user for current age
current_age = int(input('How old are you? '))

# Calculate age in 2050 (current year is 2023, so add 27 years)
future_age = current_age + 27

# Print the result
print('In 2050, you will be {} years old'.format(future_age))
